name: Daily NBA Schedule

on:
  schedule:
    # 每天北京时间早上 8 点运行（UTC 时间 0 点）
    - cron: '0 0 * * *'
  workflow_dispatch: # 允许在 Actions 页面手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run script
        env:
          TZ: Asia/Shanghai
          SERVERCHAN_KEY: ${{ secrets.SERVERCHAN_KEY }}
        run: python main.py

      # 抓取失败时，自动打印各数据源的检测结果，方便定位是哪个源不通
      - name: Diagnose data sources
        if: failure()
        env:
          TZ: Asia/Shanghai
        run: python main.py --diagnose || true
